#include<iostream>
#include<cmath>
#include<iomanip>
using namespace std;

int main(){
    int a;
    cin >> a >> a;
    cout << fixed << setprecision(12) << acos(0.0)*2*a*a*a*4/3 << endl;
    return 0;
}